// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/tuihub/librarian/internal/data/internal/ent/predicate"
	"github.com/tuihub/librarian/internal/data/internal/ent/sentinel"
	"github.com/tuihub/librarian/internal/data/internal/ent/sentinellibrary"
	"github.com/tuihub/librarian/internal/data/internal/ent/sentinelsession"
	"github.com/tuihub/librarian/internal/model"
)

// SentinelQuery is the builder for querying Sentinel entities.
type SentinelQuery struct {
	config
	ctx                 *QueryContext
	order               []sentinel.OrderOption
	inters              []Interceptor
	predicates          []predicate.Sentinel
	withSentinelSession *SentinelSessionQuery
	withSentinelLibrary *SentinelLibraryQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the SentinelQuery builder.
func (_q *SentinelQuery) Where(ps ...predicate.Sentinel) *SentinelQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *SentinelQuery) Limit(limit int) *SentinelQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *SentinelQuery) Offset(offset int) *SentinelQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *SentinelQuery) Unique(unique bool) *SentinelQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *SentinelQuery) Order(o ...sentinel.OrderOption) *SentinelQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QuerySentinelSession chains the current query on the "sentinel_session" edge.
func (_q *SentinelQuery) QuerySentinelSession() *SentinelSessionQuery {
	query := (&SentinelSessionClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(sentinel.Table, sentinel.FieldID, selector),
			sqlgraph.To(sentinelsession.Table, sentinelsession.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, sentinel.SentinelSessionTable, sentinel.SentinelSessionColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QuerySentinelLibrary chains the current query on the "sentinel_library" edge.
func (_q *SentinelQuery) QuerySentinelLibrary() *SentinelLibraryQuery {
	query := (&SentinelLibraryClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(sentinel.Table, sentinel.FieldID, selector),
			sqlgraph.To(sentinellibrary.Table, sentinellibrary.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, sentinel.SentinelLibraryTable, sentinel.SentinelLibraryColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first Sentinel entity from the query.
// Returns a *NotFoundError when no Sentinel was found.
func (_q *SentinelQuery) First(ctx context.Context) (*Sentinel, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{sentinel.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *SentinelQuery) FirstX(ctx context.Context) *Sentinel {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Sentinel ID from the query.
// Returns a *NotFoundError when no Sentinel ID was found.
func (_q *SentinelQuery) FirstID(ctx context.Context) (id model.InternalID, err error) {
	var ids []model.InternalID
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{sentinel.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *SentinelQuery) FirstIDX(ctx context.Context) model.InternalID {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Sentinel entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Sentinel entity is found.
// Returns a *NotFoundError when no Sentinel entities are found.
func (_q *SentinelQuery) Only(ctx context.Context) (*Sentinel, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{sentinel.Label}
	default:
		return nil, &NotSingularError{sentinel.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *SentinelQuery) OnlyX(ctx context.Context) *Sentinel {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Sentinel ID in the query.
// Returns a *NotSingularError when more than one Sentinel ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *SentinelQuery) OnlyID(ctx context.Context) (id model.InternalID, err error) {
	var ids []model.InternalID
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{sentinel.Label}
	default:
		err = &NotSingularError{sentinel.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *SentinelQuery) OnlyIDX(ctx context.Context) model.InternalID {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of Sentinels.
func (_q *SentinelQuery) All(ctx context.Context) ([]*Sentinel, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*Sentinel, *SentinelQuery]()
	return withInterceptors[[]*Sentinel](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *SentinelQuery) AllX(ctx context.Context) []*Sentinel {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Sentinel IDs.
func (_q *SentinelQuery) IDs(ctx context.Context) (ids []model.InternalID, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(sentinel.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *SentinelQuery) IDsX(ctx context.Context) []model.InternalID {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *SentinelQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*SentinelQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *SentinelQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *SentinelQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *SentinelQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the SentinelQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *SentinelQuery) Clone() *SentinelQuery {
	if _q == nil {
		return nil
	}
	return &SentinelQuery{
		config:              _q.config,
		ctx:                 _q.ctx.Clone(),
		order:               append([]sentinel.OrderOption{}, _q.order...),
		inters:              append([]Interceptor{}, _q.inters...),
		predicates:          append([]predicate.Sentinel{}, _q.predicates...),
		withSentinelSession: _q.withSentinelSession.Clone(),
		withSentinelLibrary: _q.withSentinelLibrary.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithSentinelSession tells the query-builder to eager-load the nodes that are connected to
// the "sentinel_session" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *SentinelQuery) WithSentinelSession(opts ...func(*SentinelSessionQuery)) *SentinelQuery {
	query := (&SentinelSessionClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withSentinelSession = query
	return _q
}

// WithSentinelLibrary tells the query-builder to eager-load the nodes that are connected to
// the "sentinel_library" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *SentinelQuery) WithSentinelLibrary(opts ...func(*SentinelLibraryQuery)) *SentinelQuery {
	query := (&SentinelLibraryClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withSentinelLibrary = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Name string `json:"name,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.Sentinel.Query().
//		GroupBy(sentinel.FieldName).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *SentinelQuery) GroupBy(field string, fields ...string) *SentinelGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &SentinelGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = sentinel.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Name string `json:"name,omitempty"`
//	}
//
//	client.Sentinel.Query().
//		Select(sentinel.FieldName).
//		Scan(ctx, &v)
func (_q *SentinelQuery) Select(fields ...string) *SentinelSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &SentinelSelect{SentinelQuery: _q}
	sbuild.label = sentinel.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a SentinelSelect configured with the given aggregations.
func (_q *SentinelQuery) Aggregate(fns ...AggregateFunc) *SentinelSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *SentinelQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !sentinel.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *SentinelQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Sentinel, error) {
	var (
		nodes       = []*Sentinel{}
		_spec       = _q.querySpec()
		loadedTypes = [2]bool{
			_q.withSentinelSession != nil,
			_q.withSentinelLibrary != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Sentinel).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Sentinel{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withSentinelSession; query != nil {
		if err := _q.loadSentinelSession(ctx, query, nodes,
			func(n *Sentinel) { n.Edges.SentinelSession = []*SentinelSession{} },
			func(n *Sentinel, e *SentinelSession) { n.Edges.SentinelSession = append(n.Edges.SentinelSession, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withSentinelLibrary; query != nil {
		if err := _q.loadSentinelLibrary(ctx, query, nodes,
			func(n *Sentinel) { n.Edges.SentinelLibrary = []*SentinelLibrary{} },
			func(n *Sentinel, e *SentinelLibrary) { n.Edges.SentinelLibrary = append(n.Edges.SentinelLibrary, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *SentinelQuery) loadSentinelSession(ctx context.Context, query *SentinelSessionQuery, nodes []*Sentinel, init func(*Sentinel), assign func(*Sentinel, *SentinelSession)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[model.InternalID]*Sentinel)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(sentinelsession.FieldSentinelID)
	}
	query.Where(predicate.SentinelSession(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(sentinel.SentinelSessionColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.SentinelID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "sentinel_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *SentinelQuery) loadSentinelLibrary(ctx context.Context, query *SentinelLibraryQuery, nodes []*Sentinel, init func(*Sentinel), assign func(*Sentinel, *SentinelLibrary)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[model.InternalID]*Sentinel)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(sentinellibrary.FieldSentinelID)
	}
	query.Where(predicate.SentinelLibrary(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(sentinel.SentinelLibraryColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.SentinelID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "sentinel_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (_q *SentinelQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *SentinelQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(sentinel.Table, sentinel.Columns, sqlgraph.NewFieldSpec(sentinel.FieldID, field.TypeInt64))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, sentinel.FieldID)
		for i := range fields {
			if fields[i] != sentinel.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *SentinelQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(sentinel.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = sentinel.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// SentinelGroupBy is the group-by builder for Sentinel entities.
type SentinelGroupBy struct {
	selector
	build *SentinelQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *SentinelGroupBy) Aggregate(fns ...AggregateFunc) *SentinelGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *SentinelGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SentinelQuery, *SentinelGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *SentinelGroupBy) sqlScan(ctx context.Context, root *SentinelQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// SentinelSelect is the builder for selecting fields of Sentinel entities.
type SentinelSelect struct {
	*SentinelQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *SentinelSelect) Aggregate(fns ...AggregateFunc) *SentinelSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *SentinelSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SentinelQuery, *SentinelSelect](ctx, _s.SentinelQuery, _s, _s.inters, v)
}

func (_s *SentinelSelect) sqlScan(ctx context.Context, root *SentinelQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
