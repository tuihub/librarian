// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/tuihub/librarian/internal/data/internal/ent/predicate"
	"github.com/tuihub/librarian/internal/data/internal/ent/sentinelinfo"
	"github.com/tuihub/librarian/internal/data/internal/ent/sentinellibrary"
	"github.com/tuihub/librarian/internal/model"
)

// SentinelInfoQuery is the builder for querying SentinelInfo entities.
type SentinelInfoQuery struct {
	config
	ctx                 *QueryContext
	order               []sentinelinfo.OrderOption
	inters              []Interceptor
	predicates          []predicate.SentinelInfo
	withSentinelLibrary *SentinelLibraryQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the SentinelInfoQuery builder.
func (siq *SentinelInfoQuery) Where(ps ...predicate.SentinelInfo) *SentinelInfoQuery {
	siq.predicates = append(siq.predicates, ps...)
	return siq
}

// Limit the number of records to be returned by this query.
func (siq *SentinelInfoQuery) Limit(limit int) *SentinelInfoQuery {
	siq.ctx.Limit = &limit
	return siq
}

// Offset to start from.
func (siq *SentinelInfoQuery) Offset(offset int) *SentinelInfoQuery {
	siq.ctx.Offset = &offset
	return siq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (siq *SentinelInfoQuery) Unique(unique bool) *SentinelInfoQuery {
	siq.ctx.Unique = &unique
	return siq
}

// Order specifies how the records should be ordered.
func (siq *SentinelInfoQuery) Order(o ...sentinelinfo.OrderOption) *SentinelInfoQuery {
	siq.order = append(siq.order, o...)
	return siq
}

// QuerySentinelLibrary chains the current query on the "sentinel_library" edge.
func (siq *SentinelInfoQuery) QuerySentinelLibrary() *SentinelLibraryQuery {
	query := (&SentinelLibraryClient{config: siq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := siq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := siq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(sentinelinfo.Table, sentinelinfo.FieldID, selector),
			sqlgraph.To(sentinellibrary.Table, sentinellibrary.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, true, sentinelinfo.SentinelLibraryTable, sentinelinfo.SentinelLibraryColumn),
		)
		fromU = sqlgraph.SetNeighbors(siq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first SentinelInfo entity from the query.
// Returns a *NotFoundError when no SentinelInfo was found.
func (siq *SentinelInfoQuery) First(ctx context.Context) (*SentinelInfo, error) {
	nodes, err := siq.Limit(1).All(setContextOp(ctx, siq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{sentinelinfo.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (siq *SentinelInfoQuery) FirstX(ctx context.Context) *SentinelInfo {
	node, err := siq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first SentinelInfo ID from the query.
// Returns a *NotFoundError when no SentinelInfo ID was found.
func (siq *SentinelInfoQuery) FirstID(ctx context.Context) (id model.InternalID, err error) {
	var ids []model.InternalID
	if ids, err = siq.Limit(1).IDs(setContextOp(ctx, siq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{sentinelinfo.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (siq *SentinelInfoQuery) FirstIDX(ctx context.Context) model.InternalID {
	id, err := siq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single SentinelInfo entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one SentinelInfo entity is found.
// Returns a *NotFoundError when no SentinelInfo entities are found.
func (siq *SentinelInfoQuery) Only(ctx context.Context) (*SentinelInfo, error) {
	nodes, err := siq.Limit(2).All(setContextOp(ctx, siq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{sentinelinfo.Label}
	default:
		return nil, &NotSingularError{sentinelinfo.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (siq *SentinelInfoQuery) OnlyX(ctx context.Context) *SentinelInfo {
	node, err := siq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only SentinelInfo ID in the query.
// Returns a *NotSingularError when more than one SentinelInfo ID is found.
// Returns a *NotFoundError when no entities are found.
func (siq *SentinelInfoQuery) OnlyID(ctx context.Context) (id model.InternalID, err error) {
	var ids []model.InternalID
	if ids, err = siq.Limit(2).IDs(setContextOp(ctx, siq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{sentinelinfo.Label}
	default:
		err = &NotSingularError{sentinelinfo.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (siq *SentinelInfoQuery) OnlyIDX(ctx context.Context) model.InternalID {
	id, err := siq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of SentinelInfos.
func (siq *SentinelInfoQuery) All(ctx context.Context) ([]*SentinelInfo, error) {
	ctx = setContextOp(ctx, siq.ctx, ent.OpQueryAll)
	if err := siq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*SentinelInfo, *SentinelInfoQuery]()
	return withInterceptors[[]*SentinelInfo](ctx, siq, qr, siq.inters)
}

// AllX is like All, but panics if an error occurs.
func (siq *SentinelInfoQuery) AllX(ctx context.Context) []*SentinelInfo {
	nodes, err := siq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of SentinelInfo IDs.
func (siq *SentinelInfoQuery) IDs(ctx context.Context) (ids []model.InternalID, err error) {
	if siq.ctx.Unique == nil && siq.path != nil {
		siq.Unique(true)
	}
	ctx = setContextOp(ctx, siq.ctx, ent.OpQueryIDs)
	if err = siq.Select(sentinelinfo.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (siq *SentinelInfoQuery) IDsX(ctx context.Context) []model.InternalID {
	ids, err := siq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (siq *SentinelInfoQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, siq.ctx, ent.OpQueryCount)
	if err := siq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, siq, querierCount[*SentinelInfoQuery](), siq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (siq *SentinelInfoQuery) CountX(ctx context.Context) int {
	count, err := siq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (siq *SentinelInfoQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, siq.ctx, ent.OpQueryExist)
	switch _, err := siq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (siq *SentinelInfoQuery) ExistX(ctx context.Context) bool {
	exist, err := siq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the SentinelInfoQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (siq *SentinelInfoQuery) Clone() *SentinelInfoQuery {
	if siq == nil {
		return nil
	}
	return &SentinelInfoQuery{
		config:              siq.config,
		ctx:                 siq.ctx.Clone(),
		order:               append([]sentinelinfo.OrderOption{}, siq.order...),
		inters:              append([]Interceptor{}, siq.inters...),
		predicates:          append([]predicate.SentinelInfo{}, siq.predicates...),
		withSentinelLibrary: siq.withSentinelLibrary.Clone(),
		// clone intermediate query.
		sql:  siq.sql.Clone(),
		path: siq.path,
	}
}

// WithSentinelLibrary tells the query-builder to eager-load the nodes that are connected to
// the "sentinel_library" edge. The optional arguments are used to configure the query builder of the edge.
func (siq *SentinelInfoQuery) WithSentinelLibrary(opts ...func(*SentinelLibraryQuery)) *SentinelInfoQuery {
	query := (&SentinelLibraryClient{config: siq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	siq.withSentinelLibrary = query
	return siq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		URL string `json:"url,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.SentinelInfo.Query().
//		GroupBy(sentinelinfo.FieldURL).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (siq *SentinelInfoQuery) GroupBy(field string, fields ...string) *SentinelInfoGroupBy {
	siq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &SentinelInfoGroupBy{build: siq}
	grbuild.flds = &siq.ctx.Fields
	grbuild.label = sentinelinfo.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		URL string `json:"url,omitempty"`
//	}
//
//	client.SentinelInfo.Query().
//		Select(sentinelinfo.FieldURL).
//		Scan(ctx, &v)
func (siq *SentinelInfoQuery) Select(fields ...string) *SentinelInfoSelect {
	siq.ctx.Fields = append(siq.ctx.Fields, fields...)
	sbuild := &SentinelInfoSelect{SentinelInfoQuery: siq}
	sbuild.label = sentinelinfo.Label
	sbuild.flds, sbuild.scan = &siq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a SentinelInfoSelect configured with the given aggregations.
func (siq *SentinelInfoQuery) Aggregate(fns ...AggregateFunc) *SentinelInfoSelect {
	return siq.Select().Aggregate(fns...)
}

func (siq *SentinelInfoQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range siq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, siq); err != nil {
				return err
			}
		}
	}
	for _, f := range siq.ctx.Fields {
		if !sentinelinfo.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if siq.path != nil {
		prev, err := siq.path(ctx)
		if err != nil {
			return err
		}
		siq.sql = prev
	}
	return nil
}

func (siq *SentinelInfoQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*SentinelInfo, error) {
	var (
		nodes       = []*SentinelInfo{}
		_spec       = siq.querySpec()
		loadedTypes = [1]bool{
			siq.withSentinelLibrary != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*SentinelInfo).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &SentinelInfo{config: siq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, siq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := siq.withSentinelLibrary; query != nil {
		if err := siq.loadSentinelLibrary(ctx, query, nodes,
			func(n *SentinelInfo) { n.Edges.SentinelLibrary = []*SentinelLibrary{} },
			func(n *SentinelInfo, e *SentinelLibrary) {
				n.Edges.SentinelLibrary = append(n.Edges.SentinelLibrary, e)
			}); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (siq *SentinelInfoQuery) loadSentinelLibrary(ctx context.Context, query *SentinelLibraryQuery, nodes []*SentinelInfo, init func(*SentinelInfo), assign func(*SentinelInfo, *SentinelLibrary)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[model.InternalID]*SentinelInfo)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(sentinellibrary.FieldSentinelInfoID)
	}
	query.Where(predicate.SentinelLibrary(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(sentinelinfo.SentinelLibraryColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.SentinelInfoID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "sentinel_info_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (siq *SentinelInfoQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := siq.querySpec()
	_spec.Node.Columns = siq.ctx.Fields
	if len(siq.ctx.Fields) > 0 {
		_spec.Unique = siq.ctx.Unique != nil && *siq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, siq.driver, _spec)
}

func (siq *SentinelInfoQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(sentinelinfo.Table, sentinelinfo.Columns, sqlgraph.NewFieldSpec(sentinelinfo.FieldID, field.TypeInt64))
	_spec.From = siq.sql
	if unique := siq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if siq.path != nil {
		_spec.Unique = true
	}
	if fields := siq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, sentinelinfo.FieldID)
		for i := range fields {
			if fields[i] != sentinelinfo.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := siq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := siq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := siq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := siq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (siq *SentinelInfoQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(siq.driver.Dialect())
	t1 := builder.Table(sentinelinfo.Table)
	columns := siq.ctx.Fields
	if len(columns) == 0 {
		columns = sentinelinfo.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if siq.sql != nil {
		selector = siq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if siq.ctx.Unique != nil && *siq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range siq.predicates {
		p(selector)
	}
	for _, p := range siq.order {
		p(selector)
	}
	if offset := siq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := siq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// SentinelInfoGroupBy is the group-by builder for SentinelInfo entities.
type SentinelInfoGroupBy struct {
	selector
	build *SentinelInfoQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (sigb *SentinelInfoGroupBy) Aggregate(fns ...AggregateFunc) *SentinelInfoGroupBy {
	sigb.fns = append(sigb.fns, fns...)
	return sigb
}

// Scan applies the selector query and scans the result into the given value.
func (sigb *SentinelInfoGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, sigb.build.ctx, ent.OpQueryGroupBy)
	if err := sigb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SentinelInfoQuery, *SentinelInfoGroupBy](ctx, sigb.build, sigb, sigb.build.inters, v)
}

func (sigb *SentinelInfoGroupBy) sqlScan(ctx context.Context, root *SentinelInfoQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(sigb.fns))
	for _, fn := range sigb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*sigb.flds)+len(sigb.fns))
		for _, f := range *sigb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*sigb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := sigb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// SentinelInfoSelect is the builder for selecting fields of SentinelInfo entities.
type SentinelInfoSelect struct {
	*SentinelInfoQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (sis *SentinelInfoSelect) Aggregate(fns ...AggregateFunc) *SentinelInfoSelect {
	sis.fns = append(sis.fns, fns...)
	return sis
}

// Scan applies the selector query and scans the result into the given value.
func (sis *SentinelInfoSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, sis.ctx, ent.OpQuerySelect)
	if err := sis.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SentinelInfoQuery, *SentinelInfoSelect](ctx, sis.SentinelInfoQuery, sis, sis.inters, v)
}

func (sis *SentinelInfoSelect) sqlScan(ctx context.Context, root *SentinelInfoQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(sis.fns))
	for _, fn := range sis.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*sis.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := sis.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
