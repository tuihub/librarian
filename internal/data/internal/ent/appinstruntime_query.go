// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/tuihub/librarian/internal/data/internal/ent/appinstruntime"
	"github.com/tuihub/librarian/internal/data/internal/ent/predicate"
)

// AppInstRunTimeQuery is the builder for querying AppInstRunTime entities.
type AppInstRunTimeQuery struct {
	config
	ctx        *QueryContext
	order      []appinstruntime.OrderOption
	inters     []Interceptor
	predicates []predicate.AppInstRunTime
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the AppInstRunTimeQuery builder.
func (airtq *AppInstRunTimeQuery) Where(ps ...predicate.AppInstRunTime) *AppInstRunTimeQuery {
	airtq.predicates = append(airtq.predicates, ps...)
	return airtq
}

// Limit the number of records to be returned by this query.
func (airtq *AppInstRunTimeQuery) Limit(limit int) *AppInstRunTimeQuery {
	airtq.ctx.Limit = &limit
	return airtq
}

// Offset to start from.
func (airtq *AppInstRunTimeQuery) Offset(offset int) *AppInstRunTimeQuery {
	airtq.ctx.Offset = &offset
	return airtq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (airtq *AppInstRunTimeQuery) Unique(unique bool) *AppInstRunTimeQuery {
	airtq.ctx.Unique = &unique
	return airtq
}

// Order specifies how the records should be ordered.
func (airtq *AppInstRunTimeQuery) Order(o ...appinstruntime.OrderOption) *AppInstRunTimeQuery {
	airtq.order = append(airtq.order, o...)
	return airtq
}

// First returns the first AppInstRunTime entity from the query.
// Returns a *NotFoundError when no AppInstRunTime was found.
func (airtq *AppInstRunTimeQuery) First(ctx context.Context) (*AppInstRunTime, error) {
	nodes, err := airtq.Limit(1).All(setContextOp(ctx, airtq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{appinstruntime.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (airtq *AppInstRunTimeQuery) FirstX(ctx context.Context) *AppInstRunTime {
	node, err := airtq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first AppInstRunTime ID from the query.
// Returns a *NotFoundError when no AppInstRunTime ID was found.
func (airtq *AppInstRunTimeQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = airtq.Limit(1).IDs(setContextOp(ctx, airtq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{appinstruntime.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (airtq *AppInstRunTimeQuery) FirstIDX(ctx context.Context) int {
	id, err := airtq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single AppInstRunTime entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one AppInstRunTime entity is found.
// Returns a *NotFoundError when no AppInstRunTime entities are found.
func (airtq *AppInstRunTimeQuery) Only(ctx context.Context) (*AppInstRunTime, error) {
	nodes, err := airtq.Limit(2).All(setContextOp(ctx, airtq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{appinstruntime.Label}
	default:
		return nil, &NotSingularError{appinstruntime.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (airtq *AppInstRunTimeQuery) OnlyX(ctx context.Context) *AppInstRunTime {
	node, err := airtq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only AppInstRunTime ID in the query.
// Returns a *NotSingularError when more than one AppInstRunTime ID is found.
// Returns a *NotFoundError when no entities are found.
func (airtq *AppInstRunTimeQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = airtq.Limit(2).IDs(setContextOp(ctx, airtq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{appinstruntime.Label}
	default:
		err = &NotSingularError{appinstruntime.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (airtq *AppInstRunTimeQuery) OnlyIDX(ctx context.Context) int {
	id, err := airtq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of AppInstRunTimes.
func (airtq *AppInstRunTimeQuery) All(ctx context.Context) ([]*AppInstRunTime, error) {
	ctx = setContextOp(ctx, airtq.ctx, ent.OpQueryAll)
	if err := airtq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*AppInstRunTime, *AppInstRunTimeQuery]()
	return withInterceptors[[]*AppInstRunTime](ctx, airtq, qr, airtq.inters)
}

// AllX is like All, but panics if an error occurs.
func (airtq *AppInstRunTimeQuery) AllX(ctx context.Context) []*AppInstRunTime {
	nodes, err := airtq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of AppInstRunTime IDs.
func (airtq *AppInstRunTimeQuery) IDs(ctx context.Context) (ids []int, err error) {
	if airtq.ctx.Unique == nil && airtq.path != nil {
		airtq.Unique(true)
	}
	ctx = setContextOp(ctx, airtq.ctx, ent.OpQueryIDs)
	if err = airtq.Select(appinstruntime.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (airtq *AppInstRunTimeQuery) IDsX(ctx context.Context) []int {
	ids, err := airtq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (airtq *AppInstRunTimeQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, airtq.ctx, ent.OpQueryCount)
	if err := airtq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, airtq, querierCount[*AppInstRunTimeQuery](), airtq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (airtq *AppInstRunTimeQuery) CountX(ctx context.Context) int {
	count, err := airtq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (airtq *AppInstRunTimeQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, airtq.ctx, ent.OpQueryExist)
	switch _, err := airtq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (airtq *AppInstRunTimeQuery) ExistX(ctx context.Context) bool {
	exist, err := airtq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the AppInstRunTimeQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (airtq *AppInstRunTimeQuery) Clone() *AppInstRunTimeQuery {
	if airtq == nil {
		return nil
	}
	return &AppInstRunTimeQuery{
		config:     airtq.config,
		ctx:        airtq.ctx.Clone(),
		order:      append([]appinstruntime.OrderOption{}, airtq.order...),
		inters:     append([]Interceptor{}, airtq.inters...),
		predicates: append([]predicate.AppInstRunTime{}, airtq.predicates...),
		// clone intermediate query.
		sql:  airtq.sql.Clone(),
		path: airtq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		UserID model.InternalID `json:"user_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.AppInstRunTime.Query().
//		GroupBy(appinstruntime.FieldUserID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (airtq *AppInstRunTimeQuery) GroupBy(field string, fields ...string) *AppInstRunTimeGroupBy {
	airtq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &AppInstRunTimeGroupBy{build: airtq}
	grbuild.flds = &airtq.ctx.Fields
	grbuild.label = appinstruntime.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		UserID model.InternalID `json:"user_id,omitempty"`
//	}
//
//	client.AppInstRunTime.Query().
//		Select(appinstruntime.FieldUserID).
//		Scan(ctx, &v)
func (airtq *AppInstRunTimeQuery) Select(fields ...string) *AppInstRunTimeSelect {
	airtq.ctx.Fields = append(airtq.ctx.Fields, fields...)
	sbuild := &AppInstRunTimeSelect{AppInstRunTimeQuery: airtq}
	sbuild.label = appinstruntime.Label
	sbuild.flds, sbuild.scan = &airtq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a AppInstRunTimeSelect configured with the given aggregations.
func (airtq *AppInstRunTimeQuery) Aggregate(fns ...AggregateFunc) *AppInstRunTimeSelect {
	return airtq.Select().Aggregate(fns...)
}

func (airtq *AppInstRunTimeQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range airtq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, airtq); err != nil {
				return err
			}
		}
	}
	for _, f := range airtq.ctx.Fields {
		if !appinstruntime.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if airtq.path != nil {
		prev, err := airtq.path(ctx)
		if err != nil {
			return err
		}
		airtq.sql = prev
	}
	return nil
}

func (airtq *AppInstRunTimeQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*AppInstRunTime, error) {
	var (
		nodes = []*AppInstRunTime{}
		_spec = airtq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*AppInstRunTime).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &AppInstRunTime{config: airtq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, airtq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (airtq *AppInstRunTimeQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := airtq.querySpec()
	_spec.Node.Columns = airtq.ctx.Fields
	if len(airtq.ctx.Fields) > 0 {
		_spec.Unique = airtq.ctx.Unique != nil && *airtq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, airtq.driver, _spec)
}

func (airtq *AppInstRunTimeQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(appinstruntime.Table, appinstruntime.Columns, sqlgraph.NewFieldSpec(appinstruntime.FieldID, field.TypeInt))
	_spec.From = airtq.sql
	if unique := airtq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if airtq.path != nil {
		_spec.Unique = true
	}
	if fields := airtq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, appinstruntime.FieldID)
		for i := range fields {
			if fields[i] != appinstruntime.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := airtq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := airtq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := airtq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := airtq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (airtq *AppInstRunTimeQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(airtq.driver.Dialect())
	t1 := builder.Table(appinstruntime.Table)
	columns := airtq.ctx.Fields
	if len(columns) == 0 {
		columns = appinstruntime.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if airtq.sql != nil {
		selector = airtq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if airtq.ctx.Unique != nil && *airtq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range airtq.predicates {
		p(selector)
	}
	for _, p := range airtq.order {
		p(selector)
	}
	if offset := airtq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := airtq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// AppInstRunTimeGroupBy is the group-by builder for AppInstRunTime entities.
type AppInstRunTimeGroupBy struct {
	selector
	build *AppInstRunTimeQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (airtgb *AppInstRunTimeGroupBy) Aggregate(fns ...AggregateFunc) *AppInstRunTimeGroupBy {
	airtgb.fns = append(airtgb.fns, fns...)
	return airtgb
}

// Scan applies the selector query and scans the result into the given value.
func (airtgb *AppInstRunTimeGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, airtgb.build.ctx, ent.OpQueryGroupBy)
	if err := airtgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*AppInstRunTimeQuery, *AppInstRunTimeGroupBy](ctx, airtgb.build, airtgb, airtgb.build.inters, v)
}

func (airtgb *AppInstRunTimeGroupBy) sqlScan(ctx context.Context, root *AppInstRunTimeQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(airtgb.fns))
	for _, fn := range airtgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*airtgb.flds)+len(airtgb.fns))
		for _, f := range *airtgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*airtgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := airtgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// AppInstRunTimeSelect is the builder for selecting fields of AppInstRunTime entities.
type AppInstRunTimeSelect struct {
	*AppInstRunTimeQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (airts *AppInstRunTimeSelect) Aggregate(fns ...AggregateFunc) *AppInstRunTimeSelect {
	airts.fns = append(airts.fns, fns...)
	return airts
}

// Scan applies the selector query and scans the result into the given value.
func (airts *AppInstRunTimeSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, airts.ctx, ent.OpQuerySelect)
	if err := airts.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*AppInstRunTimeQuery, *AppInstRunTimeSelect](ctx, airts.AppInstRunTimeQuery, airts, airts.inters, v)
}

func (airts *AppInstRunTimeSelect) sqlScan(ctx context.Context, root *AppInstRunTimeQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(airts.fns))
	for _, fn := range airts.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*airts.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := airts.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
