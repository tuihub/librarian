// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/tuihub/librarian/internal/data/internal/ent/predicate"
	"github.com/tuihub/librarian/internal/data/internal/ent/sentinel"
	"github.com/tuihub/librarian/internal/data/internal/ent/sentinelsession"
	"github.com/tuihub/librarian/internal/model"
)

// SentinelSessionQuery is the builder for querying SentinelSession entities.
type SentinelSessionQuery struct {
	config
	ctx          *QueryContext
	order        []sentinelsession.OrderOption
	inters       []Interceptor
	predicates   []predicate.SentinelSession
	withSentinel *SentinelQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the SentinelSessionQuery builder.
func (ssq *SentinelSessionQuery) Where(ps ...predicate.SentinelSession) *SentinelSessionQuery {
	ssq.predicates = append(ssq.predicates, ps...)
	return ssq
}

// Limit the number of records to be returned by this query.
func (ssq *SentinelSessionQuery) Limit(limit int) *SentinelSessionQuery {
	ssq.ctx.Limit = &limit
	return ssq
}

// Offset to start from.
func (ssq *SentinelSessionQuery) Offset(offset int) *SentinelSessionQuery {
	ssq.ctx.Offset = &offset
	return ssq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (ssq *SentinelSessionQuery) Unique(unique bool) *SentinelSessionQuery {
	ssq.ctx.Unique = &unique
	return ssq
}

// Order specifies how the records should be ordered.
func (ssq *SentinelSessionQuery) Order(o ...sentinelsession.OrderOption) *SentinelSessionQuery {
	ssq.order = append(ssq.order, o...)
	return ssq
}

// QuerySentinel chains the current query on the "sentinel" edge.
func (ssq *SentinelSessionQuery) QuerySentinel() *SentinelQuery {
	query := (&SentinelClient{config: ssq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := ssq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := ssq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(sentinelsession.Table, sentinelsession.FieldID, selector),
			sqlgraph.To(sentinel.Table, sentinel.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, sentinelsession.SentinelTable, sentinelsession.SentinelColumn),
		)
		fromU = sqlgraph.SetNeighbors(ssq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first SentinelSession entity from the query.
// Returns a *NotFoundError when no SentinelSession was found.
func (ssq *SentinelSessionQuery) First(ctx context.Context) (*SentinelSession, error) {
	nodes, err := ssq.Limit(1).All(setContextOp(ctx, ssq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{sentinelsession.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (ssq *SentinelSessionQuery) FirstX(ctx context.Context) *SentinelSession {
	node, err := ssq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first SentinelSession ID from the query.
// Returns a *NotFoundError when no SentinelSession ID was found.
func (ssq *SentinelSessionQuery) FirstID(ctx context.Context) (id model.InternalID, err error) {
	var ids []model.InternalID
	if ids, err = ssq.Limit(1).IDs(setContextOp(ctx, ssq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{sentinelsession.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (ssq *SentinelSessionQuery) FirstIDX(ctx context.Context) model.InternalID {
	id, err := ssq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single SentinelSession entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one SentinelSession entity is found.
// Returns a *NotFoundError when no SentinelSession entities are found.
func (ssq *SentinelSessionQuery) Only(ctx context.Context) (*SentinelSession, error) {
	nodes, err := ssq.Limit(2).All(setContextOp(ctx, ssq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{sentinelsession.Label}
	default:
		return nil, &NotSingularError{sentinelsession.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (ssq *SentinelSessionQuery) OnlyX(ctx context.Context) *SentinelSession {
	node, err := ssq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only SentinelSession ID in the query.
// Returns a *NotSingularError when more than one SentinelSession ID is found.
// Returns a *NotFoundError when no entities are found.
func (ssq *SentinelSessionQuery) OnlyID(ctx context.Context) (id model.InternalID, err error) {
	var ids []model.InternalID
	if ids, err = ssq.Limit(2).IDs(setContextOp(ctx, ssq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{sentinelsession.Label}
	default:
		err = &NotSingularError{sentinelsession.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (ssq *SentinelSessionQuery) OnlyIDX(ctx context.Context) model.InternalID {
	id, err := ssq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of SentinelSessions.
func (ssq *SentinelSessionQuery) All(ctx context.Context) ([]*SentinelSession, error) {
	ctx = setContextOp(ctx, ssq.ctx, ent.OpQueryAll)
	if err := ssq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*SentinelSession, *SentinelSessionQuery]()
	return withInterceptors[[]*SentinelSession](ctx, ssq, qr, ssq.inters)
}

// AllX is like All, but panics if an error occurs.
func (ssq *SentinelSessionQuery) AllX(ctx context.Context) []*SentinelSession {
	nodes, err := ssq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of SentinelSession IDs.
func (ssq *SentinelSessionQuery) IDs(ctx context.Context) (ids []model.InternalID, err error) {
	if ssq.ctx.Unique == nil && ssq.path != nil {
		ssq.Unique(true)
	}
	ctx = setContextOp(ctx, ssq.ctx, ent.OpQueryIDs)
	if err = ssq.Select(sentinelsession.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (ssq *SentinelSessionQuery) IDsX(ctx context.Context) []model.InternalID {
	ids, err := ssq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (ssq *SentinelSessionQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, ssq.ctx, ent.OpQueryCount)
	if err := ssq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, ssq, querierCount[*SentinelSessionQuery](), ssq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (ssq *SentinelSessionQuery) CountX(ctx context.Context) int {
	count, err := ssq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (ssq *SentinelSessionQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, ssq.ctx, ent.OpQueryExist)
	switch _, err := ssq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (ssq *SentinelSessionQuery) ExistX(ctx context.Context) bool {
	exist, err := ssq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the SentinelSessionQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (ssq *SentinelSessionQuery) Clone() *SentinelSessionQuery {
	if ssq == nil {
		return nil
	}
	return &SentinelSessionQuery{
		config:       ssq.config,
		ctx:          ssq.ctx.Clone(),
		order:        append([]sentinelsession.OrderOption{}, ssq.order...),
		inters:       append([]Interceptor{}, ssq.inters...),
		predicates:   append([]predicate.SentinelSession{}, ssq.predicates...),
		withSentinel: ssq.withSentinel.Clone(),
		// clone intermediate query.
		sql:  ssq.sql.Clone(),
		path: ssq.path,
	}
}

// WithSentinel tells the query-builder to eager-load the nodes that are connected to
// the "sentinel" edge. The optional arguments are used to configure the query builder of the edge.
func (ssq *SentinelSessionQuery) WithSentinel(opts ...func(*SentinelQuery)) *SentinelSessionQuery {
	query := (&SentinelClient{config: ssq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	ssq.withSentinel = query
	return ssq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		SentinelID model.InternalID `json:"sentinel_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.SentinelSession.Query().
//		GroupBy(sentinelsession.FieldSentinelID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (ssq *SentinelSessionQuery) GroupBy(field string, fields ...string) *SentinelSessionGroupBy {
	ssq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &SentinelSessionGroupBy{build: ssq}
	grbuild.flds = &ssq.ctx.Fields
	grbuild.label = sentinelsession.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		SentinelID model.InternalID `json:"sentinel_id,omitempty"`
//	}
//
//	client.SentinelSession.Query().
//		Select(sentinelsession.FieldSentinelID).
//		Scan(ctx, &v)
func (ssq *SentinelSessionQuery) Select(fields ...string) *SentinelSessionSelect {
	ssq.ctx.Fields = append(ssq.ctx.Fields, fields...)
	sbuild := &SentinelSessionSelect{SentinelSessionQuery: ssq}
	sbuild.label = sentinelsession.Label
	sbuild.flds, sbuild.scan = &ssq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a SentinelSessionSelect configured with the given aggregations.
func (ssq *SentinelSessionQuery) Aggregate(fns ...AggregateFunc) *SentinelSessionSelect {
	return ssq.Select().Aggregate(fns...)
}

func (ssq *SentinelSessionQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range ssq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, ssq); err != nil {
				return err
			}
		}
	}
	for _, f := range ssq.ctx.Fields {
		if !sentinelsession.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if ssq.path != nil {
		prev, err := ssq.path(ctx)
		if err != nil {
			return err
		}
		ssq.sql = prev
	}
	return nil
}

func (ssq *SentinelSessionQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*SentinelSession, error) {
	var (
		nodes       = []*SentinelSession{}
		_spec       = ssq.querySpec()
		loadedTypes = [1]bool{
			ssq.withSentinel != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*SentinelSession).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &SentinelSession{config: ssq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, ssq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := ssq.withSentinel; query != nil {
		if err := ssq.loadSentinel(ctx, query, nodes, nil,
			func(n *SentinelSession, e *Sentinel) { n.Edges.Sentinel = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (ssq *SentinelSessionQuery) loadSentinel(ctx context.Context, query *SentinelQuery, nodes []*SentinelSession, init func(*SentinelSession), assign func(*SentinelSession, *Sentinel)) error {
	ids := make([]model.InternalID, 0, len(nodes))
	nodeids := make(map[model.InternalID][]*SentinelSession)
	for i := range nodes {
		fk := nodes[i].SentinelID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(sentinel.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "sentinel_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (ssq *SentinelSessionQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := ssq.querySpec()
	_spec.Node.Columns = ssq.ctx.Fields
	if len(ssq.ctx.Fields) > 0 {
		_spec.Unique = ssq.ctx.Unique != nil && *ssq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, ssq.driver, _spec)
}

func (ssq *SentinelSessionQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(sentinelsession.Table, sentinelsession.Columns, sqlgraph.NewFieldSpec(sentinelsession.FieldID, field.TypeInt64))
	_spec.From = ssq.sql
	if unique := ssq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if ssq.path != nil {
		_spec.Unique = true
	}
	if fields := ssq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, sentinelsession.FieldID)
		for i := range fields {
			if fields[i] != sentinelsession.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if ssq.withSentinel != nil {
			_spec.Node.AddColumnOnce(sentinelsession.FieldSentinelID)
		}
	}
	if ps := ssq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := ssq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := ssq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := ssq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (ssq *SentinelSessionQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(ssq.driver.Dialect())
	t1 := builder.Table(sentinelsession.Table)
	columns := ssq.ctx.Fields
	if len(columns) == 0 {
		columns = sentinelsession.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if ssq.sql != nil {
		selector = ssq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if ssq.ctx.Unique != nil && *ssq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range ssq.predicates {
		p(selector)
	}
	for _, p := range ssq.order {
		p(selector)
	}
	if offset := ssq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := ssq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// SentinelSessionGroupBy is the group-by builder for SentinelSession entities.
type SentinelSessionGroupBy struct {
	selector
	build *SentinelSessionQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (ssgb *SentinelSessionGroupBy) Aggregate(fns ...AggregateFunc) *SentinelSessionGroupBy {
	ssgb.fns = append(ssgb.fns, fns...)
	return ssgb
}

// Scan applies the selector query and scans the result into the given value.
func (ssgb *SentinelSessionGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ssgb.build.ctx, ent.OpQueryGroupBy)
	if err := ssgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SentinelSessionQuery, *SentinelSessionGroupBy](ctx, ssgb.build, ssgb, ssgb.build.inters, v)
}

func (ssgb *SentinelSessionGroupBy) sqlScan(ctx context.Context, root *SentinelSessionQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(ssgb.fns))
	for _, fn := range ssgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*ssgb.flds)+len(ssgb.fns))
		for _, f := range *ssgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*ssgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ssgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// SentinelSessionSelect is the builder for selecting fields of SentinelSession entities.
type SentinelSessionSelect struct {
	*SentinelSessionQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (sss *SentinelSessionSelect) Aggregate(fns ...AggregateFunc) *SentinelSessionSelect {
	sss.fns = append(sss.fns, fns...)
	return sss
}

// Scan applies the selector query and scans the result into the given value.
func (sss *SentinelSessionSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, sss.ctx, ent.OpQuerySelect)
	if err := sss.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SentinelSessionQuery, *SentinelSessionSelect](ctx, sss.SentinelSessionQuery, sss, sss.inters, v)
}

func (sss *SentinelSessionSelect) sqlScan(ctx context.Context, root *SentinelSessionQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(sss.fns))
	for _, fn := range sss.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*sss.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := sss.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
