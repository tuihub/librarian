// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/tuihub/librarian/app/sephirah/internal/data/internal/ent/predicate"
	"github.com/tuihub/librarian/app/sephirah/internal/data/internal/ent/systemnotification"
	"github.com/tuihub/librarian/internal/model"
)

// SystemNotificationQuery is the builder for querying SystemNotification entities.
type SystemNotificationQuery struct {
	config
	ctx        *QueryContext
	order      []systemnotification.OrderOption
	inters     []Interceptor
	predicates []predicate.SystemNotification
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the SystemNotificationQuery builder.
func (snq *SystemNotificationQuery) Where(ps ...predicate.SystemNotification) *SystemNotificationQuery {
	snq.predicates = append(snq.predicates, ps...)
	return snq
}

// Limit the number of records to be returned by this query.
func (snq *SystemNotificationQuery) Limit(limit int) *SystemNotificationQuery {
	snq.ctx.Limit = &limit
	return snq
}

// Offset to start from.
func (snq *SystemNotificationQuery) Offset(offset int) *SystemNotificationQuery {
	snq.ctx.Offset = &offset
	return snq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (snq *SystemNotificationQuery) Unique(unique bool) *SystemNotificationQuery {
	snq.ctx.Unique = &unique
	return snq
}

// Order specifies how the records should be ordered.
func (snq *SystemNotificationQuery) Order(o ...systemnotification.OrderOption) *SystemNotificationQuery {
	snq.order = append(snq.order, o...)
	return snq
}

// First returns the first SystemNotification entity from the query.
// Returns a *NotFoundError when no SystemNotification was found.
func (snq *SystemNotificationQuery) First(ctx context.Context) (*SystemNotification, error) {
	nodes, err := snq.Limit(1).All(setContextOp(ctx, snq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{systemnotification.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (snq *SystemNotificationQuery) FirstX(ctx context.Context) *SystemNotification {
	node, err := snq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first SystemNotification ID from the query.
// Returns a *NotFoundError when no SystemNotification ID was found.
func (snq *SystemNotificationQuery) FirstID(ctx context.Context) (id model.InternalID, err error) {
	var ids []model.InternalID
	if ids, err = snq.Limit(1).IDs(setContextOp(ctx, snq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{systemnotification.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (snq *SystemNotificationQuery) FirstIDX(ctx context.Context) model.InternalID {
	id, err := snq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single SystemNotification entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one SystemNotification entity is found.
// Returns a *NotFoundError when no SystemNotification entities are found.
func (snq *SystemNotificationQuery) Only(ctx context.Context) (*SystemNotification, error) {
	nodes, err := snq.Limit(2).All(setContextOp(ctx, snq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{systemnotification.Label}
	default:
		return nil, &NotSingularError{systemnotification.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (snq *SystemNotificationQuery) OnlyX(ctx context.Context) *SystemNotification {
	node, err := snq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only SystemNotification ID in the query.
// Returns a *NotSingularError when more than one SystemNotification ID is found.
// Returns a *NotFoundError when no entities are found.
func (snq *SystemNotificationQuery) OnlyID(ctx context.Context) (id model.InternalID, err error) {
	var ids []model.InternalID
	if ids, err = snq.Limit(2).IDs(setContextOp(ctx, snq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{systemnotification.Label}
	default:
		err = &NotSingularError{systemnotification.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (snq *SystemNotificationQuery) OnlyIDX(ctx context.Context) model.InternalID {
	id, err := snq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of SystemNotifications.
func (snq *SystemNotificationQuery) All(ctx context.Context) ([]*SystemNotification, error) {
	ctx = setContextOp(ctx, snq.ctx, ent.OpQueryAll)
	if err := snq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*SystemNotification, *SystemNotificationQuery]()
	return withInterceptors[[]*SystemNotification](ctx, snq, qr, snq.inters)
}

// AllX is like All, but panics if an error occurs.
func (snq *SystemNotificationQuery) AllX(ctx context.Context) []*SystemNotification {
	nodes, err := snq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of SystemNotification IDs.
func (snq *SystemNotificationQuery) IDs(ctx context.Context) (ids []model.InternalID, err error) {
	if snq.ctx.Unique == nil && snq.path != nil {
		snq.Unique(true)
	}
	ctx = setContextOp(ctx, snq.ctx, ent.OpQueryIDs)
	if err = snq.Select(systemnotification.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (snq *SystemNotificationQuery) IDsX(ctx context.Context) []model.InternalID {
	ids, err := snq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (snq *SystemNotificationQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, snq.ctx, ent.OpQueryCount)
	if err := snq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, snq, querierCount[*SystemNotificationQuery](), snq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (snq *SystemNotificationQuery) CountX(ctx context.Context) int {
	count, err := snq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (snq *SystemNotificationQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, snq.ctx, ent.OpQueryExist)
	switch _, err := snq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (snq *SystemNotificationQuery) ExistX(ctx context.Context) bool {
	exist, err := snq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the SystemNotificationQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (snq *SystemNotificationQuery) Clone() *SystemNotificationQuery {
	if snq == nil {
		return nil
	}
	return &SystemNotificationQuery{
		config:     snq.config,
		ctx:        snq.ctx.Clone(),
		order:      append([]systemnotification.OrderOption{}, snq.order...),
		inters:     append([]Interceptor{}, snq.inters...),
		predicates: append([]predicate.SystemNotification{}, snq.predicates...),
		// clone intermediate query.
		sql:  snq.sql.Clone(),
		path: snq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		UserID model.InternalID `json:"user_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.SystemNotification.Query().
//		GroupBy(systemnotification.FieldUserID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (snq *SystemNotificationQuery) GroupBy(field string, fields ...string) *SystemNotificationGroupBy {
	snq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &SystemNotificationGroupBy{build: snq}
	grbuild.flds = &snq.ctx.Fields
	grbuild.label = systemnotification.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		UserID model.InternalID `json:"user_id,omitempty"`
//	}
//
//	client.SystemNotification.Query().
//		Select(systemnotification.FieldUserID).
//		Scan(ctx, &v)
func (snq *SystemNotificationQuery) Select(fields ...string) *SystemNotificationSelect {
	snq.ctx.Fields = append(snq.ctx.Fields, fields...)
	sbuild := &SystemNotificationSelect{SystemNotificationQuery: snq}
	sbuild.label = systemnotification.Label
	sbuild.flds, sbuild.scan = &snq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a SystemNotificationSelect configured with the given aggregations.
func (snq *SystemNotificationQuery) Aggregate(fns ...AggregateFunc) *SystemNotificationSelect {
	return snq.Select().Aggregate(fns...)
}

func (snq *SystemNotificationQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range snq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, snq); err != nil {
				return err
			}
		}
	}
	for _, f := range snq.ctx.Fields {
		if !systemnotification.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if snq.path != nil {
		prev, err := snq.path(ctx)
		if err != nil {
			return err
		}
		snq.sql = prev
	}
	return nil
}

func (snq *SystemNotificationQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*SystemNotification, error) {
	var (
		nodes = []*SystemNotification{}
		_spec = snq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*SystemNotification).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &SystemNotification{config: snq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, snq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (snq *SystemNotificationQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := snq.querySpec()
	_spec.Node.Columns = snq.ctx.Fields
	if len(snq.ctx.Fields) > 0 {
		_spec.Unique = snq.ctx.Unique != nil && *snq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, snq.driver, _spec)
}

func (snq *SystemNotificationQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(systemnotification.Table, systemnotification.Columns, sqlgraph.NewFieldSpec(systemnotification.FieldID, field.TypeInt64))
	_spec.From = snq.sql
	if unique := snq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if snq.path != nil {
		_spec.Unique = true
	}
	if fields := snq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, systemnotification.FieldID)
		for i := range fields {
			if fields[i] != systemnotification.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := snq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := snq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := snq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := snq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (snq *SystemNotificationQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(snq.driver.Dialect())
	t1 := builder.Table(systemnotification.Table)
	columns := snq.ctx.Fields
	if len(columns) == 0 {
		columns = systemnotification.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if snq.sql != nil {
		selector = snq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if snq.ctx.Unique != nil && *snq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range snq.predicates {
		p(selector)
	}
	for _, p := range snq.order {
		p(selector)
	}
	if offset := snq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := snq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// SystemNotificationGroupBy is the group-by builder for SystemNotification entities.
type SystemNotificationGroupBy struct {
	selector
	build *SystemNotificationQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (sngb *SystemNotificationGroupBy) Aggregate(fns ...AggregateFunc) *SystemNotificationGroupBy {
	sngb.fns = append(sngb.fns, fns...)
	return sngb
}

// Scan applies the selector query and scans the result into the given value.
func (sngb *SystemNotificationGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, sngb.build.ctx, ent.OpQueryGroupBy)
	if err := sngb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SystemNotificationQuery, *SystemNotificationGroupBy](ctx, sngb.build, sngb, sngb.build.inters, v)
}

func (sngb *SystemNotificationGroupBy) sqlScan(ctx context.Context, root *SystemNotificationQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(sngb.fns))
	for _, fn := range sngb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*sngb.flds)+len(sngb.fns))
		for _, f := range *sngb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*sngb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := sngb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// SystemNotificationSelect is the builder for selecting fields of SystemNotification entities.
type SystemNotificationSelect struct {
	*SystemNotificationQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (sns *SystemNotificationSelect) Aggregate(fns ...AggregateFunc) *SystemNotificationSelect {
	sns.fns = append(sns.fns, fns...)
	return sns
}

// Scan applies the selector query and scans the result into the given value.
func (sns *SystemNotificationSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, sns.ctx, ent.OpQuerySelect)
	if err := sns.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SystemNotificationQuery, *SystemNotificationSelect](ctx, sns.SystemNotificationQuery, sns, sns.inters, v)
}

func (sns *SystemNotificationSelect) sqlScan(ctx context.Context, root *SystemNotificationQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(sns.fns))
	for _, fn := range sns.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*sns.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := sns.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
